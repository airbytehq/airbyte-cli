version: 2

project_name: airbyte-agent

before:
  hooks:
    - go mod tidy

builds:
  - id: airbyte-agent
    main: .
    binary: airbyte-agent
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/airbytehq/airbyte-agent-cli/cmd.Version={{.Version}}
      - -X github.com/airbytehq/airbyte-agent-cli/cmd.Commit={{.Commit}}
      - -X github.com/airbytehq/airbyte-agent-cli/cmd.Date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: default
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - AGENTS.md
      - CONTEXT.md

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"

release:
  draft: true
  prerelease: auto
  name_template: "v{{ .Version }}"

# Homebrew formula generation. Goreleaser commits Formula/airbyte-agent.rb to the
# airbytehq/homebrew-tap repo on each non-prerelease release. Cross-repo write
# requires a separate token (HOMEBREW_TAP_GITHUB_TOKEN) — the default
# GITHUB_TOKEN issued by the workflow only has access to this repo.
brews:
  - name: airbyte-agent
    repository:
      owner: airbytehq
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
      branch: main
    directory: Formula
    homepage: https://github.com/airbytehq/airbyte-agent-cli
    description: CLI for Airbyte Agents, designed for use by both humans and agents
    license: ELv2
    install: |
      bin.install "airbyte-agent"
    test: |
      system "#{bin}/airbyte-agent", "version"
    skip_upload: auto # don't update the formula for prereleases (e.g. v0.1.0-rc1)
